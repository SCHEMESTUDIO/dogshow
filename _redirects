# Clean dog page URLs — rewrite /d/slug to dog.html with slug param
/d/:slug  /dog.html?slug=:slug  200

# Clean SEO page URLs
# Note: this _redirects file is a Netlify/Vercel format. GitHub Pages (current host)
# ignores it — clean URLs work because filenames match (e.g. /dog-photo-contest
# resolves to dog-photo-contest.html via GH Pages' .html stripping). Rules kept
# for any future host migration.
/contest  /dog-photo-contest  301
/dog-contest  /dog-photo-contest  301
