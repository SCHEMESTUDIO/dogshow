{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/d/:slug", "destination": "/api/dog?slug=:slug" },
    { "source": "/:slug", "destination": "/:slug.html" }
  ]
}
