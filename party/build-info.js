// Auto-stamped at deploy time by stamp-version.cjs — do NOT edit by hand.
// The value committed here is a placeholder; `npm run deploy` overwrites it
// with the real git commit + timestamp (baked into the deployed bundle and
// served at GET /version), then reverts this file locally so git stays clean.
// Run `npm run check-deploy` to compare the live prod version against your repo.
export const BUILD_INFO = { commit: 'dev', dirty: false, deployedAt: null };
