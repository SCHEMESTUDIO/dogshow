{
  "name": "dogshow-party",
  "version": "1.0.0",
  "description": "PartyKit server for dogshow.lol real-time chat + bones",
  "main": "server.js",
  "scripts": {
    "dev": "npx partykit dev",
    "deploy": "node stamp-version.cjs && npx partykit deploy && git checkout -- build-info.js",
    "check-deploy": "node check-deploy.cjs"
  },
  "dependencies": {
    "partykit": "^0.0.108"
  }
}
