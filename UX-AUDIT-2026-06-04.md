# The Dog Show — UX / Flow Audit

**Date:** 2026-06-04 · **Method:** Live walkthrough of production (dogshow.lol) via browser + real emails inspected in Gmail, cross-referenced against the repo. **Scope:** every public URL and every email a user receives during/after signup.

> A PartyKit redeploy was performed mid-audit (to rule out stale-server drift). Findings below note pre- vs post-deploy state where relevant. The static frontend is on Vercel; the realtime/API/email server is on PartyKit and is deployed separately.

---

## Summary

Tested fully (live): all public pages, free signup, magic-link login, welcome + login emails, unsubscribe endpoint, **a real $3.99 BYD purchase end-to-end (payment → provisioning → upload → certificate), and the scheduled-slot RSVP + reminder flow.**
Not live-tested: bones top-up ($1.99), out-of-bones, 2nd-dog 409 (code-traced only).

**Severity tally:** 1 High open (+1 High found-and-fixed) · 2 Medium · ~9 Low/Minor.

The single most important theme: **the deployed PartyKit server can drift from your repo.** The magic-link email failure was caused by this and was fixed by redeploying — but the redeploy did **not** fix the unsubscribe link, which means that one is a genuine current-code/bundle issue, not staleness.

**The BYD happy path works** end-to-end: real Stripe payment → `verify-checkout` provisioning → "You're in" email → username step → dog upload → AI classifier correctly tagged "Chocolate Lab" (0.93) → certificate email → pre-show page → RSVP → 5-min reminder email. The issues below are around the edges of that path, not the spine of it.

---

## HIGH

### H1 — Unsubscribe link broken in every email (CAN-SPAM) ✅ FIXED & VERIFIED END-TO-END
The delivered link was malformed — the **second** query param's `=` (and a token char) were corrupted in the mail pipeline: `?u=…&tD961…` (dropped) or even `?u=…&t[7603…` (the `=` turned into `[`) instead of `&t=<16-hex>` — so unsubscribe failed for *every* email.
- **Diagnosis:** a temporary `/debug-unsub` probe (since removed) proved the deployed code generates a *clean* `&t=`+16-hex URL — so the corruption is in **email delivery (Resend)**, not the code. HTML-escaping the `&`→`&amp;` did **not** fix it (the `=` was still eaten), ruling out HTML-attribute parsing. The *first* param's `=` always survived; only the *second* param's was hit.
- **Fix:** collapse the link to a single opaque param `?x=<userId>.<token>` (no second param, so no second `=` to corrupt); endpoint still accepts the legacy `?u=..&t=..`.
- **Verified live:** a fresh welcome email delivered `…/unsubscribe?x=user_wdvjhm.40c32c55b504b3be` **intact** (16-hex token, `=` present), and clicking it returned *"You're unsubscribed."* Done.

### H2 — Magic-link login email not delivered  → **FIXED during audit**
**Confidence: High.** Before the redeploy, a registered account requested a login link and none arrived (the welcome email to the same inbox arrived in seconds). After redeploy, the same pre-existing account received its link and logged in successfully end-to-end. Root cause was server drift (stale build not sending / not indexing). **Verify it stays fixed**, and confirm no pre-deploy real customers are stuck (spot-check that an older paid account can request a link and receive it).

---

## MEDIUM

### M1 — Geo consent gating was completely non-functional (lost ROW ad data) ✅ FIXED & VERIFIED
**The bug:** the committed/deployed middleware was `middleware.mjs`, but **Vercel only runs middleware named `.ts/.tsx/.js/.jsx`** — a `.mjs` is silently served as a static file and never executes. So `dogshow_geo` was **never set** and `consent.js` hit its EU fail-safe for *every* visitor regardless of location. Impact: US/ROW visitors got the banner + trackers gated **off** → GA4/Bing UET conversions not captured → undercounted ad-trial data. (My initial "US visitor" framing was off because the operator is UK-based — the EU banner looked correct for them, masking that ROW visitors were mis-gated.)
**The fix (shipped, commit `0260f37`):** renamed to `middleware.ts` + added to `tsconfig` include, removed `middleware.mjs`. **Verified live:** `dogshow_geo` cookie is now set; a real GB request correctly returns `EU` (banner shown). Non-GDPR visitors now get `ROW` (no banner, trackers on). *Worth a one-time spot-check from a US IP/VPN or Vercel logs to confirm `ROW` end-to-end, but the mechanism is confirmed running.*

### M2 — success.html swallows payment-verification failures ✅ FIXED
**Confidence: High (code + live).** `success.html` defaulted `tier` to `'general'` when params were missing and silently redirected to `show.html?topup=1`; a failed `verify-checkout` was only `console.error`'d. Fixed: `registerUser()` now returns success/failure, and a `showVerifyError()` state appears on any failure (or missing `session_id`) — a calm, accurate message ("If you were charged, your access activates within a few minutes — reply to your Stripe receipt and we'll sort it out") instead of a silent redirect. Bones-pack buyers are now only redirected to the show on actual success.
- Still open (separate): `app.js` has no `?topup=1` handler, so the intended "+250 bones" confirmation toast doesn't fire. Minor; left for later.

### M3 — BYD dog upload is gated behind the "Pick a name" username modal (abandonment = paid-but-no-dog) ✅ FIXED (deployed; full live re-test pending)
**Confidence: High (reproduced live in the real purchase).** After paying, the dog is **not** uploaded on the success page. It uploads only after the buyer enters the show **and** clears the mandatory "Pick a name" username modal — at which point `/upload-dog` fires (confirmed 200). In the window before that, `/my-dog` returns `dog: null` and the stashed photo is already cleared from `localStorage`. So if a premium buyer closes the tab at the username modal (or it errors), **their dog is never created and won't retry on next visit** — they're a paid account with no dog. This is almost certainly a source of the "stuck paid users" your admin tooling exists to resolve.
- Compounding it: `success.html` already shows **"✓ Entered into The Dog Show"** *before* the dog actually exists — a premature confirmation.
- **Action:** persist the pending dog until `/upload-dog` actually succeeds (don't clear on read), and/or submit the dog immediately on `verify-checkout` rather than gating it behind the username step. At minimum, don't claim "entered" until the server confirms it.

---

## LOW / MINOR

### L1 — `login.html` dead-ends on a stale/expired link ✅ FIXED
Visiting `/login` with no/expired token showed *"Invalid login link…"* with no way to recover. Fixed: all three error states now reveal a **"Request a new link →"** button that deep-links to the landing-page sign-in form (`/#loginSection`).

### L2 — Magic-link email footer is invisible (white-on-white) ✅ FIXED
The login email had a light background but reused the dark-email footer (white text), making the unsubscribe link + address unreadable. Fixed: the login email is now dark-themed (`#1a1035`) like the welcome/certificate emails, so the footer is legible.

### L3 — Login email copy says "expires in 15 minutes" but the token is valid 60 minutes ✅ FIXED
Copy corrected to "60 minutes" to match the token TTL.

### L4 — At least one aired dog has `slug: null` ("Skeeterino") ✅ FIX SHIPPED (run once)
An early upload (pre slug-guarantee) has no slug → no SSR `/d/{slug}` cert + no per-dog OG image. Added a one-shot admin endpoint **`GET /admin-backfill-slugs?key=<ADMIN_KEY>`** (dry-run; add `&commit=1` to write) that assigns a slug to any slug-less community dog and stores the `slug:` mapping. **Action: run it once with `commit=1`** after deploy to fix Skeeterino. (L5's fallback already keeps its gallery card working in the meantime.)

### L5 — Gallery linked to client-rendered `dog.html?id=…`, not the canonical SSR `/d/{slug}` ✅ FIXED
`dogs.html` gallery cards now link to `/d/{slug}` when a slug exists (share-optimized + SEO), falling back to `?id=` only for slug-less dogs.

### L6 — ~5s blank "Waiting for the show to start…" on first show load ✅ IMPROVED
The stage is briefly blank until the first WebSocket `sync`. Swapped the placeholder copy to on-brand *"The curtain is about to rise…"* so the wait reads as intentional. (A seeded/last-known dog would be a larger follow-up if desired.)

### L7 — Cookie banner intercepting the hero CTA ✅ NOT A BUG (verified)
The consent banner is `position:fixed; bottom:0` — a bottom bar that doesn't overlap the mid-page hero CTA, and modals open above it fine. The earlier "click didn't open" was test-harness coordinate imprecision, not a real block. No change needed.

### L8 — Certificate email is wrong for *scheduled* dogs ✅ FIXED
A dog booked for a future slot got the certificate email **immediately** on upload, with copy that said *"now in the rotation, collecting bones"* and a **"View certificate"** link — but the dog hadn't aired, and that link leads to the **pre-show countdown** page. Fixed in `sendCertificateEmail` (`party/server.js`): scheduled dogs now get scheduled-aware copy — subject "🗓️ … is booked", eyebrow "Booked", "booked for {UTC time}", a "View … page" CTA, and an "RSVP and cheer them on" share line. Immediate ("show now") uploads are unchanged. The upload handler now passes `slotAt`/`firstAppearedAt` to the email so the branch can fire. **Needs a PartyKit deploy.**

### L9 — Duplicate RSVP reminder email ✅ FIXED
The reminder was delivered **twice** (same second). Root cause: in `processSlotReminders`, the 1-hour reminder fired for *any* slot `<= 60 min` out with no lower bound, so a slot booked under ~6 min out satisfied both the 1-hr and 5-min checks in one alarm pass and sent two identical emails. Fixed: the 1-hour reminder now only fires while **more than 5 minutes** remain, so it can't collapse into the 5-minute one.

### L10 — RSVP promised a "1 hour before" reminder that can't happen for near-term slots ✅ FIXED
The confirmation copy always said *"an hour and 5 minutes before."* Softened (in `api/dog.js`, both the pre-show sub-copy and the success message) to *"We'll email you a reminder before they take the stage"* — accurate regardless of how far out the slot is.

### L11 — RSVP sends a second "Welcome" email — mitigated by L9
RSVP auto-registers the email (→ Welcome email) on top of the reminder. With L9's duplicate gone, an RSVP now yields **two** emails (welcome + one reminder), which is reasonable — no separate change made. Revisit (suppress the welcome on RSVP-origin registration) only if it still feels like too much.

### L12 — RSVP overwrites the current session token ✅ FIXED
`api/dog.js` set `dogshow_token` to the RSVP-issued (new free) account unconditionally, so a **logged-in owner who RSVPs to any dog gets silently swapped off their own account**. Fixed: the RSVP session is now only adopted for a logged-*out* visitor (`!localStorage.getItem('dogshow_token')`); an existing session is never overwritten.

---

## What works well (verified live)
- Landing, show, gallery, breed hubs (e.g. `/breeds/bernedoodle`), SEO pages, about/privacy/terms — all render cleanly.
- `/d/{slug}` SSR certificate renders correctly; invalid slug → graceful "this dog could not be found"; bad URL → friendly 404.
- **Free signup → 250 bones → show is flawless.** Welcome email is well-built (aside from H1's unsubscribe link).
- Magic-link verify → session → show works end-to-end (post-deploy).
- **Real $3.99 BYD purchase works end-to-end:** Stripe payment → `verify-checkout` → "You're in" email → username → `/upload-dog` (200) → certificate email. The **AI breed classifier works** (tagged the photo "Chocolate Lab", 0.93 confidence).
- **Scheduled-slot pre-show page** (`/d/{slug}`) is excellent: booked time, live countdown, RSVP form, share buttons. RSVP records correctly and the **5-minute reminder email fires**.
- PartyKit connects; live chat + responsive bot working.

---

## Not live-tested (code-traced only)
- **Bones top-up ($1.99), out-of-bones (`needTopUp`), one-dog-per-account 409.** The 409 is documented and the account now has a dog (Louise), so it's set up to verify; out-of-bones needs a balance drained to 0. Prod-may-differ caveat applies, but the rest of the server behaved per repo once redeployed.

---

## Test data created (for cleanup)
- Free test accounts in PartyKit storage: `james.lamon+dogaudit@gmail.com` (user_f5td8u), `james.lamon+audit3@gmail.com` (user_kiruu1), `james.lamon+rsvptest@gmail.com`, possibly `+audit2`.
- **Paid premium account:** `james.lamon+audit4@gmail.com` (user, $3.99 real charge — refund via Stripe if desired) with dog **"Louise"** (`slug: louise`, scheduled to air Jun 4 ~2:30 PM UTC). Louise is a real entry now visible on the live site.
- `Dog Show/IMG_4827.jpg` (test upload photo) — safe to delete.

## Recommended priority order
1. **H1** unsubscribe (legal) → 2. **M3** stuck-paid-user upload gate (revenue/CX) → 3. **M1** geo/ad-data → 4. **M2** success-page silent failure → 5. confirm **H2** stays fixed → 6. the L-items as polish (L8 scheduled-cert-email and L9 duplicate reminder are the most user-visible).
And: **establish a deploy check** so PartyKit prod can't silently drift from the repo (the root of H1/H2).
